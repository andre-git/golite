# schema package

Defines the in-memory schema model used by the SQL generator and the DB layer. It tracks tables, columns, and indexes and mirrors SQLite metadata.

## Files

- `schema.go`: schema, table, column, and index structures.
